{% extends 'base.html.twig' %}

{% block titre_page %} <strong class="text-primary">Modification Nombre de confirmation</strong> {% endblock %}
{% block body %}
    <div class="row">
        <div class="col-md-6">
            <div class="box">

                <div class="box-header with-border">
                    <h3 class="box-title">Formulaire nombre de confirmation</h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    {{ form_start(edit_form) }}
                    {{ form_widget(edit_form) }}

                    <br>
                    <div class="row">
                        <div class="col-md-6">
                            <input type="submit" value="Modifier" class="btn btn-primary btn-block" />
                        </div>
                        <div class="col-md-6">
                            <a href="{{ path('nombreconfirmation_index') }}" class="btn btn-default btn-block">Annuler</a>
                        </div>

                    </div>
                    {{ form_end(edit_form) }}
                </div>
            </div>
        </div>

    </div>

{% endblock %}
