{% extends 'base.html.twig' %}

{% block body %}
    <h1>Listepiece</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ listePiece.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ listePiece.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('listepiece_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('listepiece_edit', { 'id': listePiece.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
